$("#new_list").remove();
$("#add_new_list").show();
$('.all_lists').append("<%= j render 'list', partial_list: @list %>");


// $('.all_lists').append('<li id="list_<%= @list.id %>" ><%= link_to @list.title, user_list_path(current_user, @list) %> <%= link_to "(remove)", user_list_path(current_user, @list), method: :delete, remote: true, class: "remove_list" %></li>');


$(".lists_counter_title").html('Item has been added<br/>Now you have <%= pluralize(@lists.size, "item") %>');