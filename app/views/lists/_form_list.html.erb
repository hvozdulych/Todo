<%= form_for @list, url: user_lists_path, remote: true do |f| %>
  <%= f.text_field :title %>
  <%= f.submit "ssave", class: "new_list_btn" %>
<% end %>
<p class="error-message" style="color: red;"><%= @list.errors.full_messages.first if @list.errors.any? %></p>